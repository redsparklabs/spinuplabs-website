= render 'feed_link'
%section.content
	.container
		- if collection.present?
			= render collection, :summary => true
			= paginate collection
		- else
			No posts found.
%section.work
	.container
		.logos
