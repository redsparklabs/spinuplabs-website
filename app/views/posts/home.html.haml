= render 'feed_link'
%section.banner
	.container
		.banner-image
		.banner-intro
			%h2.cta
				SpinUp Labs is a Rails (Rails) and CodeIgniter (PHP) Development Shop
			%p
				Our focus is to build well crafted web applications. We specialize in test-driven development, growing teams through training, and finding solutions for our clients.
			%p
				%h3.cta
					We're easy to work with!
				%button
					Let's talk about your project
			%p
				Call Toll-Free (866) 985-6877

%section.content
	.container
		- if collection.present?
			= render collection, :summary => true
			= paginate collection
		- else
			No posts found.
%section.work
	.container
		.logos
